<Bindings>
    <Binding name="Toggle Worldmap" runOnUp="true" header="thpack.mapmark">
        if keystate == "down" then
            if not InCombatLockdown() then
                ShowUIPanel(WorldMapFrame)
            end
        else
            HideUIPanel(WorldMapFrame)
        end
    </Binding>
    <Binding name="Toggle Scoreboard" runOnUp="true" header="thpack.scoreboard">
        if (keystate == "down") then
            showScoreboard();
        else
            hideScoreboard();
        end
    </Binding>
</Bindings>
