<Bindings>
    <Binding name="THPACK_TOGGLE_MAP" runOnUp="true" header="THPACK_BIND">
        if keystate == "down" then
            if not InCombatLockdown() then
                ShowUIPanel(WorldMapFrame)
            end
        else
            HideUIPanel(WorldMapFrame)
        end
    </Binding>
    <Binding name="THPACK_TOGGLE_SCOREBOARD" runOnUp="true">
        if (keystate == "down") then
            showScoreboard();
        else
            hideScoreboard();
        end
    </Binding>
</Bindings>
